const config = {
    // Define your Node.js server URL and port
    serverUrl: 'http://localhost', // Update with your server's URL
    serverPort: 3000, // Update with your server's port
};

export default config;
