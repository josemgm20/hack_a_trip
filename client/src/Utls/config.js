const config = {
    // Define la URL y el puerto de tu servidor Node.js
    serverUrl: 'http://localhost', // Actualiza con la URL de tu servidor
    serverPort: 3000, // Actualiza con el puerto de tu servidor
};

export default config;
